# ─────────────────────────────────────────────────────────────────────────────
# Default judge chain — tried in order until one succeeds.
#
# Each entry references a profile file (without .yaml extension) in this dir.
# Profiles earlier in the list have higher priority.
# ─────────────────────────────────────────────────────────────────────────────

# DeepSeek first: V3.2-Exp chat is ~$0.27/$1.10 per 1M tokens, reasoning strong,
# 64K context. Cheap off-peak discount available (16:30-00:30 UTC).
- deepseek

# NIM second: best latency + GPQA 86.7% (confirmed Sep 2026 integration scan)
- nim

# OpenRouter free tier: Nemotron 550B + DeepSeek V3 (no-cost backup)
- openrouter

# Gemini API: Google AI Studio (final fallback, may incur cost)
- gemini
